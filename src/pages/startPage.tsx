import React from "react";
import { Text, View } from "react-native";

const StartPage = () => {
  return (
    <View>
      <Text>startPage works!</Text>
    </View>
  );
};

export default StartPage;
